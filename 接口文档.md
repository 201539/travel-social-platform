# 旅游社交平台 - 接口文档（Server API）

## 基础信息
- **服务端入口**：`server/index.js`
- **API Base URL**：`http://<host>:<port>`
- **API 前缀**：`/api`
- **健康检查**：`GET /health`
- **静态资源**：`/uploads/<filename>`（图片上传后返回的 URL 即指向这里）

## 鉴权与权限
- **JWT**：需要登录的接口必须在请求头带：
  - `Authorization: Bearer <token>`
- **封禁逻辑**：
  - 被封禁用户 **无法登录**（`POST /api/auth/login` 返回 403）
  - 被封禁用户 **无法访问任何需要登录的接口**（`middleware/auth.js` 返回 403，含 `bannedReason`/`bannedUntil`）
- **管理员权限**：
  - 部分接口需要 `req.user.role === 'admin'`（`middleware/requireAdmin.js`）

## 统一错误响应（部分接口会内联返回 message）
项目同时存在「路由内 try/catch 直返」与「统一错误中间件」两种风格：
- **统一错误中间件格式**（`server/middleware/errorHandler.js`）：

```json
{
  "success": false,
  "message": "服务器错误"
}
```

> 但很多路由直接返回 `{ message, error }`，文档以下以“实际路由返回”为准。

## 通用字段说明（核心模型）
### User（`server/models/User.js`）
- `username`：3-20 字符，唯一
- `email`：唯一，小写存储
- `avatar`：头像 URL（字符串）
- `bio`：最多 200 字
- `followers` / `following`：用户 ID 数组
- `travelCount`：游记数（数字）
- `role`：`user | admin`
- `isBanned` / `bannedReason` / `bannedUntil`

### Travel（`server/models/Travel.js`）
- `title`：1-100 字符（创建时强校验）
- `description`：1-2000 字符（创建时强校验；模型最大 2000）
- `author`：用户 ID
- `startDate` / `endDate`：日期（创建时要求 start <= end）
- `track`：`[[lng, lat], ...]`
- `markPoints`：标记点数组（含 `location.coordinates`、`name`、`type` 等）
- `photos`：图片 URL 数组
- `location`：GeoJSON Point（`coordinates: [lng, lat]`）
- `locationName`：地点/城市名（字符串）
- `likes` / `collections`：用户 ID 数组
- `comments`：`[{ user, content, createdAt }]`（`content` 最大 500）
- `views`：浏览量
- `tags`：字符串数组（创建/更新时支持逗号分隔字符串）

### Location（`server/models/Location.js`）
- `name`：必填
- `location.coordinates`：`[lng, lat]` 必填
- `address`：地址（可选）
- `type`：`attraction | restaurant | hotel | other`
- `travels`：关联游记 ID 数组
- `rating`：0-5
- `visitCount`：访问/关联计数

---

## 0. 健康检查
### GET `/health`
**响应示例**

```json
{
  "status": "ok",
  "message": "服务运行正常",
  "timestamp": "2026-01-20T00:00:00.000Z",
  "uptime": 123.45
}
```

---

## 1. 认证 Auth（`/api/auth`）

### 1.1 注册
- **POST** `/api/auth/register`
- **Body(JSON)**：
  - `username`（必填，3-20）
  - `email`（必填，邮箱格式）
  - `password`（必填，≥6）

**响应（201）**

```json
{
  "message": "注册成功",
  "token": "<jwt>",
  "user": {
    "id": "<userId>",
    "username": "xxx",
    "email": "xxx@example.com",
    "avatar": "",
    "bio": "",
    "role": "user"
  }
}
```

### 1.2 登录
- **POST** `/api/auth/login`
- **Body(JSON)**：
  - `email`（必填）
  - `password`（必填）

**封禁响应（403）**

```json
{
  "message": "账号已被封禁",
  "bannedReason": "xxx",
  "bannedUntil": "2026-01-20T00:00:00.000Z"
}
```

**成功响应（200）**

```json
{
  "message": "登录成功",
  "token": "<jwt>",
  "user": {
    "id": "<userId>",
    "username": "xxx",
    "email": "xxx@example.com",
    "avatar": "",
    "bio": "",
    "role": "user"
  }
}
```

---

## 2. 用户 Users（`/api/users`）

### 2.1 获取用户信息
- **GET** `/api/users/:id`
- **鉴权**：否
- **响应**：用户对象（不含 password），并 `populate followers/following`（字段：`username avatar`）

### 2.2 更新用户信息
- **PUT** `/api/users/:id`
- **鉴权**：是（且只能修改自己）
- **Body(JSON)**（可选字段）：
  - `username`
  - `bio`（可传空字符串）
  - `avatar`

**响应示例**

```json
{
  "message": "更新成功",
  "user": { "_id": "<userId>" }
}
```

### 2.3 管理员：封禁用户
- **POST** `/api/users/:id/ban`
- **鉴权**：是（管理员）
- **Body(JSON)**（可选）：
  - `bannedReason`（字符串，后端截断至 200）
  - `bannedUntil`（日期字符串或时间戳；不传表示永久/不设截止）

### 2.4 管理员：解封用户
- **POST** `/api/users/:id/unban`
- **鉴权**：是（管理员）

### 2.5 搜索用户
- **GET** `/api/users/search/:keyword`
- **鉴权**：否
- **说明**：按 `username` 或 `email` 模糊搜索（不区分大小写），最多 20 条

---

## 3. 游记 Travels（`/api/travels`）

### 3.1 创建游记
- **POST** `/api/travels`
- **鉴权**：是
- **Body(JSON)**（必填）：
  - `title`（1-100）
  - `description`（1-2000）
  - `startDate`（可被 `new Date()` 解析）
  - `endDate`（可被 `new Date()` 解析，且 ≥ startDate）
- **Body(JSON)**（可选）：
  - `track`：`[[lng,lat], ...]`
  - `markPoints`：标记点数组
  - `photos`：图片 URL 数组
  - `locationName`：城市/地点名
  - `location`：`[lng, lat]`（注意：这里是数组；后端会转换成 GeoJSON）
  - `tags`：数组 或 逗号分隔字符串

**响应（201）**：`{ message, travel }`

### 3.2 获取游记列表（分页/筛选）
- **GET** `/api/travels`
- **Query**：
  - `page`（默认 1）
  - `limit`（默认 20）
  - `author`（作者 userId）
  - `keyword`：只匹配 `locationName`，并且是 **完全匹配**（正则 `^keyword$`，忽略大小写）

**响应**

```json
{
  "travels": [],
  "totalPages": 1,
  "currentPage": 1,
  "total": 0
}
```

> 为提升性能：列表接口会 `select('-track -markPoints')`，不返回轨迹与标记点。

### 3.3 获取单个游记
- **GET** `/api/travels/:id`
- **鉴权**：否
- **说明**：
  - `populate author/likes/comments.user`
  - 会异步 `views +1`

### 3.4 更新游记
- **PUT** `/api/travels/:id`
- **鉴权**：是（作者本人）
- **Body(JSON)**（仅允许以下字段）：
  - `title`, `description`, `startDate`, `endDate`, `photos`, `tags`, `locationName`, `location`
- **tags 特殊处理**：若传字符串，则按逗号切分
- **location 特殊处理**：若传 `[lng,lat]` 则转换为 GeoJSON

### 3.5 删除游记
- **DELETE** `/api/travels/:id`
- **鉴权**：是（作者或管理员）

### 3.6 点赞/取消点赞
- **POST** `/api/travels/:id/like`
- **鉴权**：是
- **说明**：已点赞则取消，否则点赞

### 3.7 评论
- **POST** `/api/travels/:id/comment`
- **鉴权**：是
- **Body(JSON)**：
  - `content`（必填）

### 3.8 收藏/取消收藏
- **POST** `/api/travels/:id/collect`
- **鉴权**：是
- **说明**：已收藏则取消，否则收藏

---

## 4. 地点 Locations（`/api/locations`）

### 4.1 获取地点详情（含该地点的游记）
- **GET** `/api/locations/:id`
- **鉴权**：否
- **说明**：会 `populate travels`，并对 travel 的 `author` 再 populate（`username avatar`）

### 4.2 附近地点搜索
- **GET** `/api/locations/search/nearby`
- **Query**：
  - `longitude`（必填）
  - `latitude`（必填）
  - `distance`（可选，默认 5000，单位米）
- **响应**：地点数组（最多 20），并 `populate travels`（字段 `title photos`）

### 4.3 按名称搜索地点
- **GET** `/api/locations/search/:keyword`
- **说明**：按 `name` 或 `address` 模糊搜索（不区分大小写），最多 20 条

### 4.4 热门地点列表
- **GET** `/api/locations/popular/list`
- **Query**：
  - `limit`（默认 20）
- **排序**：`visitCount` desc, `rating` desc

### 4.5 获取地点的所有游记
- **GET** `/api/locations/:id/travels`

---

## 5. 社交 Social（关注）（`/api/social`）

### 5.1 关注/取消关注
- **POST** `/api/social/follow/:userId`
- **鉴权**：是
- **说明**：已关注则取消，否则关注

**响应示例**

```json
{
  "message": "关注成功",
  "isFollowing": true
}
```

### 5.2 获取关注列表
- **GET** `/api/social/following/:userId`
- **鉴权**：否
- **响应**：用户数组（`username avatar bio travelCount`）

### 5.3 获取粉丝列表
- **GET** `/api/social/followers/:userId`
- **鉴权**：否

### 5.4 推荐用户
- **GET** `/api/social/recommend`
- **鉴权**：是
- **说明**：推荐“关注的人的关注的人”，并排除自己与已关注用户；返回字段 `username avatar bio travelCount followers`

---

## 6. 文件 Files（图片上传）（`/api/files`）
> 上传文件存储在服务端 `server/uploads/`，并通过 `GET /uploads/<filename>` 访问。

### 6.1 上传单张图片
- **POST** `/api/files/upload`
- **鉴权**：是
- **Content-Type**：`multipart/form-data`
- **Form-Data**：
  - `image`：图片文件（jpeg/jpg/png/gif/webp；最大 10MB）

**响应**

```json
{
  "message": "上传成功",
  "url": "http://<host>:<port>/uploads/image-xxx.jpg",
  "filename": "image-xxx.jpg"
}
```

### 6.2 上传多张图片
- **POST** `/api/files/upload/multiple`
- **鉴权**：是
- **Content-Type**：`multipart/form-data`
- **Form-Data**：
  - `images`：最多 10 张

**响应**

```json
{
  "message": "上传成功",
  "urls": ["http://<host>:<port>/uploads/xxx.jpg"]
}
```

---

## 7. 轨迹 Tracks（`/api/tracks`）

### 7.1 上传 GPS 轨迹点
- **POST** `/api/tracks/upload`
- **鉴权**：是（且必须是游记作者）
- **Body(JSON)**：
  - `travelId`（必填）
  - `points`（必填，数组）：`[[lng, lat, timestamp], ...]`

> 注意：后端只取前两项写入 `travel.track`：`[[lng,lat], ...]`，timestamp 会被丢弃。

### 7.2 获取轨迹与标记点
- **GET** `/api/tracks/:travelId`
- **鉴权**：否
- **响应**：`{ track, markPoints }`

---

## 8. 天气 Weather（`/api/weather`）
> 调用第三方「心知天气」：`https://api.seniverse.com/v3/weather/now.json`

### GET `/api/weather`
- **Query**：
  - 方案 A：`longitude` + `latitude`
  - 方案 B：`city`
- **依赖配置**：需要在 `server/.env` 配置 `WEATHER_API_KEY`

**未配置密钥（500）**

```json
{
  "message": "天气API密钥未配置，请在server/.env中配置WEATHER_API_KEY",
  "error": "API_KEY_NOT_CONFIGURED",
  "isRealData": false
}
```

**成功响应（200，免费版：仅当前天气）**

```json
{
  "location": "南京",
  "current": { "temperature": 10, "condition": "多云", "code": "4" },
  "forecast": [],
  "isRealData": true,
  "apiSource": "seniverse",
  "timestamp": "2026-01-20T00:00:00.000Z",
  "isFreePlan": true
}
```

---

## 9. 导航 Navigation（路线规划）（`/api/navigation`）
> 依赖地图服务封装：`server/services/mapService.js`（需要配置地图 API Key）

### 9.1 驾车路线规划
- **POST** `/api/navigation/route/driving`
- **Body(JSON)**：
  - `origin`: `{ longitude, latitude }`（必填）
  - `destination`: `{ longitude, latitude }`（必填）
  - `strategy`（可选，默认 `"0"`）

### 9.2 步行路线规划
- **POST** `/api/navigation/route/walking`
- **Body(JSON)**：同上（无 `strategy`）

### 9.3 公交路线规划
- **POST** `/api/navigation/route/transit`
- **Body(JSON)**：
  - `origin` / `destination`（必填）
  - `city`（可选，默认空字符串）

**成功响应格式（3 个接口一致）**

```json
{
  "message": "路线规划成功",
  "route": {}
}
```

---

## 10. AI（`/api/ai`）
### 10.1 AI 帮写游记内容
- **POST** `/api/ai/write-travel`
- **鉴权**：是
- **Body(JSON)**（可选字段）：
  - `title`
  - `locationName`
  - `startDate`
  - `endDate`
  - `tags`（数组）
  - `userPrompt`（自定义提示；不传则后端自动拼接提示词）

**响应**

```json
{
  "success": true,
  "content": "......"
}
```

### 10.2 AI 推荐餐厅
- **POST** `/api/ai/recommend-restaurants`
- **鉴权**：否
- **Body(JSON)**：
  - `city`（必填）

**响应（期望 restaurants 为数组）**

```json
{
  "success": true,
  "city": "南京",
  "restaurants": [
    { "name": "xxx", "cuisine": "本帮菜", "rating": "4.5/5.0", "location": "鼓楼区", "reason": "..." }
  ],
  "rawContent": "..."
}
```

---

## 11. 404
任意未匹配路由：
- **响应（404）**：`{ "message": "接口不存在" }`

